// cl(res.body)
    // const reader = res.body.getReader();
    // cl(res.body.locked)
    // // const file = 
    // Deno.writeFileSync('f.zip',await res.buffer());
    // // await Deno.copy(res.body, file);
    // process.stdin.pipe()
    // const outStream = new Writable({
    //     write(chunk, encoding, callback) {
    //         console.log(chunk)
    //         callback()
    //     },
    // })
    // res.body.pipe(outStream)
    // for await (const chunk of res.body!) {
    //     console.log("chunk", Buffer.from(chunk));
    //   }
    // cl(header) 
    // cl(info.signedPutUrl)